import ShareButton from '../common/ShareButton';

const HeaderWithShare = () => {
  return (
    <div className="flex items-center justify-end w-full h-16">
      <ShareButton />
    </div>
  );
};

export default HeaderWithShare;
