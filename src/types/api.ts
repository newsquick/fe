export type MessageGetResponse = {
  messageId: string;
  regDate: string;
  resultData: string;
  userName: string;
};

export type MessagePostResponse = {
  messageId: string;
  resultData: string;
};
