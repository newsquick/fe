export type MessageGetResponse = {
  messageId: string;
  shareKey: string;
  regDate: string;
  resultData: string;
  userName: string;
};

export type MessagePostResponse = {
  messageId: string;
  resultData: string;
  shareKey: string;
};
